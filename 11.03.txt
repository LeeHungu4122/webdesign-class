큐넷에 기출문제 있음!

button 은 inline 요소이므로 가온데정렬 가능.

script에서 첫번째 순번을 0으로 친다.
$("ul > li").eq(4)==> li에 해당하는 것 중에 4순번 애를 불러오는 것.

popup창을 만들 때에는 wrap 과 형제 엘리먼트로 만들자!!!!!! (아얘 바깥공간에 만들자)
popup창의 배경을 투명하게 하고싶을 땐 rgb 값을 이용하자 (opacity 값 이용하면 안에있는 내용까지 전부 투명해짐.)

가로분할되는부분에 after 해주자

borderbox 사용할 때 margin이 들어간 경우는 적용이 안된다. (padding만 가능)

class는 중복사용가능. id는 중복사용 불가능.

dl 은 dt dd를 묶어주는 느낌. 테이블과 비슷한 느낌.

nth chlid 와 nth of type 의 차이점은 child는 다른 요소가 형제로 있다면 같이 카운팅되서 순서를 정하면 되고 type는 지정한 type만 골라서 카운팅한다.

